package org.example.dp.structural.decorator;

/* 1
* To implement this feature, we can use the Decorator design pattern in Java. First, we need to define an interface or an abstract class that represents a Pizza.
*  This class should have a method that returns the base cost of the pizza.
* */
public interface Pizza {
    public double getCost();
}
