package org.example.dp.structural.decorator.examp2;

/* 1
To implement this feature, we can use the Decorator design pattern in Java. First, we need to define an interface or
an abstract class that represents a Car. This class should have a method that returns the rental cost of the car.*/
public interface Car {
    public double getRentalCost();
}
