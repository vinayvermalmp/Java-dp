package org.example.dp.structural.adapter;

/*
* Next, let's create two concrete API classes, one that returns data in the expected
*  format and one that returns data in an incompatible format:
* */
public class InCompatibleAPI {
    public String retrieveData() {
        // code to retrieve data in an incompatible format
        return  null;
    }
}
